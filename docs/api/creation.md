# Creation

Functions for constructing frame-labeled arrays. Both are re-exported at the top level, so
they are also available as `pld.from_values_and_labels` and `pld.from_frame`. For a
narrative introduction see [Eager arrays](../guide/eager-arrays.md) and
[Lazy arrays](../guide/lazy-arrays.md).

::: polder.operations.creation.from_values_and_labels

::: polder.operations.creation.from_frame
